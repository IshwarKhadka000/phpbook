apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "phpbook-chart.fullname" . }}-test-connection"
  labels:
    {{- include "phpbook-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "phpbook-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
